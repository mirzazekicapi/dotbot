{
  "name": "prod_start",
  "description": "Start the production container on the VPS (auto-stops if already running)",
  "version": "1.0.0",
  "category": "prod-workflow",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pull": {
        "type": "boolean",
        "description": "Pull latest image before starting",
        "default": false
      }
    },
    "required": [],
    "additionalProperties": false
  },
  "examples": [
    {
      "description": "Start production container",
      "input": {}
    },
    {
      "description": "Start production container with latest image",
      "input": {
        "pull": true
      }
    }
  ]
}
