name: task_get_context
description: Get pre-analysed context for a task. Used by 99-autonomous-task to retrieve packaged analysis data.
inputSchema:
  type: object
  properties:
    task_id:
      type: string
      description: "The unique identifier of the task"
  required:
    - task_id
