name: plan_create
description: Create an implementation plan for a task and link it to the task
inputSchema:
  type: object
  properties:
    task_id:
      type: string
      description: The task ID to create a plan for
    content:
      type: string
      description: The markdown content of the plan
  required: [task_id, content]
