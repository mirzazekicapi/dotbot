name: decision_mark_accepted
description: Accept a proposed Decision Record
inputSchema:
  type: object
  additionalProperties: false
  properties:
    decision_id:
      type: string
      description: The decision ID to accept (e.g. "dec-a1b2c3d4")
  required: [decision_id]
