name: task_mark_analysing
description: Move a task from todo to analysing status (pre-flight analysis phase)
inputSchema:
  type: object
  properties:
    task_id:
      type: string
      description: "The unique identifier of the task to mark as analysing"
  required:
    - task_id
