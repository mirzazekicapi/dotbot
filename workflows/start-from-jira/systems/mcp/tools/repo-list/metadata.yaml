name: repo_list
description: List all cloned repositories in repos/ with their status (cloned, branch info, analysis state).
version: 1.0.0
category: start-from-jira

inputSchema:
  type: object
  properties: {}
  required: []
  additionalProperties: false

examples:
  - description: List all cloned repos
    input: {}
